<!DOCTYPE html>
<html>
<head>
    <title>게시판</title>
    <link rel="stylesheet" href="/index.css">
</head>
<body>

<div class="header">

    <h1 class="logo">📝 자유 게시판</h1>

    <div class="nav-menu">
        <a href="/post">📝 자유게시판</a>
        <a href="/study">📘 공부기록</a>
        <a href="/todo">✅ 할일</a>
        <a href="/notice">📢 공지사항</a>
        <a href="/myPage">👤 마이페이지</a>
    </div>

</div>

<!-- 검색 -->
<form method="GET" action="/post" class="search-box">

    <select name="type">
        <option value="all" <%= type === 'all' ? 'selected' : '' %>>전체</option>
        <option value="title" <%= type === 'title' ? 'selected' : '' %>>제목</option>
        <option value="writer" <%= type === 'writer' ? 'selected' : '' %>>작성자</option>
    </select>

    <input type="text" name="keyword" value="<%= keyword %>" placeholder="검색어 입력">

    <button type="submit">검색</button>

</form>

<!-- 글쓰기 -->
<button onclick="location.href='/post/write'">글 작성</button>

<!-- 게시글 목록 -->
<table border="1" width="100%">
    <thead>
        <tr>
            <th>번호</th>
            <th>제목</th>
            <th>작성자</th>
            <th>작성일</th>
            <th>조회수</th>
        </tr>
    </thead>

    <tbody>

        <% if (!posts || posts.length === 0) { %>
            <tr>
                <td colspan="5">검색 결과가 없습니다.</td>
            </tr>
        <% } %>

        <% posts.forEach(post => { %>
            <tr>
                <td><%= post.post_id %></td>

                <td>
                    <a href="/post/<%= post.post_id %>">
                        <%= post.title %>
                    </a>
                </td>

                <td><%= post.nickName %></td>

                <td>
                    <% if (post.created_at != post.updated_at) { %>
                        수정됨: <%= new Date(post.updated_at).toLocaleString() %>
                    <% } else { %>
                        작성일: <%= new Date(post.created_at).toLocaleString() %>
                    <% } %>
                </td>

                <td><%= post.views %></td>
            </tr>
        <% }) %>

    </tbody>
</table>

<!-- 페이지네이션 -->
<div class="pagination">

<%
let blockSize = 10;
let currentBlock = Math.ceil(currentPage / blockSize);

let startPage = (currentBlock - 1) * blockSize + 1;
let endPage = startPage + blockSize - 1;

if (endPage > totalPages) {
    endPage = totalPages;
}
%>

<% if (currentPage > 1) { %>
    <a href="?page=1&type=<%= type %>&keyword=<%= keyword %>">«</a>
<% } %>

<% if (startPage > 1) { %>
    <a href="?page=<%= startPage - 1 %>&type=<%= type %>&keyword=<%= keyword %>">‹</a>
<% } %>

<% for (let i = startPage; i <= endPage; i++) { %>
    <a href="?page=<%= i %>&type=<%= type %>&keyword=<%= keyword %>"
       class="<%= currentPage === i ? 'active' : '' %>">
        <%= i %>
    </a>
<% } %>

<% if (endPage < totalPages) { %>
    <a href="?page=<%= endPage + 1 %>&type=<%= type %>&keyword=<%= keyword %>">›</a>
<% } %>

<% if (currentPage < totalPages) { %>
    <a href="?page=<%= totalPages %>&type=<%= type %>&keyword=<%= keyword %>">»</a>
<% } %>

</div>

</body>
</html>